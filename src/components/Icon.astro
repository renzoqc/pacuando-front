---
import { Icon } from 'astro-icon'
import type { HTMLAttributes } from 'astro/types'

export type Props = Omit<HTMLAttributes<'svg'>, 'slot'> & { name: string }

const { name, ...restProps } = Astro.props
---

<Icon name={name} {...restProps} />
